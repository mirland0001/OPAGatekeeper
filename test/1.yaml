# bad-capabilities-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: dangerous-pod-1
spec:
  containers:
  - name: hacker
    image: alpine
    securityContext:
      capabilities:
        add: ["NET_RAW"]  # Capacité réseau dangereuse → Doit être bloqué
